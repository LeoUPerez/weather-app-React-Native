import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    Input: {
        width: "88%",
        height: 40,
        marginTop: 12,
        borderWidth: 1,
        borderRadius: 8,
        padding: 10,
    },
    container: {
        width: '85%',
        display: 'flex',
        flexDirection: 'row',
    }
});